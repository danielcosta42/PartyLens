# PartyLens — your group radar for TBC Anniversary

Tired of staring at the LookingForGroup channel so you don't miss a tank? Tired of re-clicking the clunky Group Finder? **PartyLens watches both at once** and puts every group into one clean, searchable panel — with one-click whispers and the roles each group still needs, at a glance.

It's a lightweight LFG / group finder built specifically for the **TBC Anniversary (2.5.x)** client, where the modern dungeon finder doesn't exist.

## Features

- **Two sources, one panel** — scans the LFG chat channel *and* the built-in Group Finder together, deduplicated. Most addons do only one.
- **Role-need pips (Tank / Heal / DPS)** — color-coded, showing exactly what each group still needs, so a healer can filter to "groups that need a healer" instantly.
- **One-click whisper** — auto-fills a friendly message from your class, spec and role: `Hi! {class} {spec} {role} here. {comment}` — fully editable.
- **Rich result cards** — LFG/LFM badge, class-colored leader name, group-fill bar (e.g. 3/5, 24/25), a freshness dot, and the raw message.
- **Smart filtering** — by category (All · Dungeons · Raids · Guild · Quests · Other) and by the role you play. Search is instant and debounced.
- **Create a listing in two clicks** — pick the dungeon/raid from a live list with real names and correct IDs, no numeric activity IDs to memorize.
- **Modern "dark glass" interface** — translucent layered panels, no default WoW widgets, draggable, Escape-to-close, and it stays on screen.
- **10 languages, auto-detected** — English, Portugues, Deutsch, Francais, Espanol, Italiano, Russian, Simplified Chinese, Traditional Chinese, Korean.

## Commands

- `/partylens` — toggle the window
- `/partylens show` / `/partylens hide`
- `/partylens join` — join the LookingForGroup channel

## How it works

PartyLens auto-joins LookingForGroup on login so chat scanning works right away. Click **Update Dungeons** / **Update Raids** to refresh the official finder — the game requires that search to come from a real click, so PartyLens never spams that API in the background. Lightweight, no dependencies.

---

## Em Portugues

O PartyLens vigia o canal de LFG e o Group Finder do jogo ao mesmo tempo e junta tudo num painel unico, limpo e pesquisavel — com whisper em um clique e as roles que cada grupo precisa, num relance. Feito pro cliente **TBC Anniversary (2.5.x)**.

- **Duas fontes, um painel** — le o canal de LFG e o Group Finder juntos, sem duplicar.
- **Pips de role (Tank / Heal / DPS)** — mostram o que cada grupo precisa; filtre pela sua funcao.
- **Whisper em um clique** — mensagem montada com sua classe, spec e role, editavel.
- **Cards completos** — selo LFG/LFM, lider colorido por classe, barra de vagas, indicador de tempo.
- **Filtro inteligente** por categoria e role, com busca instantanea.
- **Criar listagem em dois cliques** escolhendo a masmorra/raide de uma lista real.
- **Interface "vidro escuro"** moderna, com 10 idiomas detectados automaticamente.

Comandos: `/partylens`, `/partylens show` / `hide`, `/partylens join`.

---

## Created by Chehul

PartyLens is made by **Chehul**. If it saves you time finding groups, you can support development:

**[Donate via PayPal](https://www.paypal.com/donate/?business=daniel.cfdutra13@gmail.com&currency_code=USD)**

Source code and issues on [GitHub](https://github.com/danielcosta42/PartyLens).
